apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-app-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: http://192.168.1.95/mohammed.zuhair/node-app-k8s.git
    targetRevision: main
    path: k8s/helm-node-app
    helm:
      releaseName: node-app-dev
      valueFiles:
        - values-dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: node-app-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true